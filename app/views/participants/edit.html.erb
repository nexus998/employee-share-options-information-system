<h1><%= t('.title') %></h1>

<%= render "form", participant: @participant %>

<br>

<div>
  <%= link_to t('shared.show'), @participant %> |
  <%= link_to t('shared.go_back'), participants_path %>
</div>
